# SQLite database (path is relative to prisma/schema.prisma)
DATABASE_URL="file:./dev.db"

# Required: use a long random string (32+ chars) in production
JWT_SECRET="replace-with-a-long-random-string"

# Docker/production example with a persistent volume mounted at /data:
# DATABASE_URL="file:/data/chata.db"
